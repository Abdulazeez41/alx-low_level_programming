#include "main.h"

/**
 *	_strlen - Count and return the length of a string.
 *	@s: integer pointer input
 *	Return: 0 if run successful
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{

	}
	return (i);
}
